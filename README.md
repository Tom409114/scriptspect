# scriptspect

Static analyzer for package.json scripts - catches shell-specific commands before they break Windows, macOS, or Linux builds. (Bootstrap placeholder; full README lands with the M0 commit.)
