{
  "name": "portable-demo",
  "private": true,
  "scripts": {
    "build": "cross-env NODE_ENV=production vite build",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "rimraf": "^6.0.1",
    "vite": "^7.0.0"
  }
}
