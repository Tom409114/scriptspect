---
name: Bug report
about: A finding that is wrong (false positive), missing (false negative), or a crash
labels: bug
---

**Rule ID (if applicable)**: e.g. PS010

**What happened?**
<!-- The exact script, the finding (or missing finding), and what you expected. -->

**package.json script (exact text)**

```json
"scripts": {
  "...": "..."
}
```

**Environment**

- OS:
- Node version:
- scriptspect version (`npx scriptspect --version`):
- Package manager (npm/pnpm/Yarn/Bun):

**Output**

```
paste output (add --format json if reproducible)
```

**Public reproduction (optional)**

A link to a public repo or CI log helps us write a regression fixture fast.
